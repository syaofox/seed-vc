# Seed-VC 训练推理完整指南

## 数据集准备要求

- 文件格式：`.wav` `.flac` `.mp3` `.m4a` `.opus` `.ogg`
- 音频长度：1-30秒
- 每位说话人至少1条语音
- 数据越多效果越好
- 建议选择干净、无背景音乐/噪音的音频

---

## V1 模型

### V1 训练

```bash
# 实时语音转换
uv run python train.py \
    --config ./configs/presets/config_dit_mel_seed_uvit_xlsr_tiny.yml \
    --dataset-dir /path/to/dataset \
    --run-name <run-name> \
    --batch-size 2 \
    --max-steps 1000 \
    --max-epochs 1000 \
    --save-every 500 \
    --num-workers 0

# 离线语音转换
uv run python train.py \
    --config ./configs/presets/config_dit_mel_seed_uvit_whisper_small_wavenet.yml \
    --dataset-dir /path/to/dataset \
    --run-name <run-name> \
    --batch-size 2 \
    --max-steps 1000 \
    --max-epochs 1000 \
    --save-every 500 \
    --num-workers 0

# 歌声转换 (44kHz)
uv run python train.py \
    --config ./configs/presets/config_dit_mel_seed_uvit_whisper_base_f0_44k.yml \
    --dataset-dir /path/to/dataset \
    --run-name <run-name> \
    --batch-size 2 \
    --max-steps 1000 \
    --max-epochs 1000 \
    --save-every 500 \
    --num-workers 0
```

### V1 检查点位置

训练完成后，检查点保存在 `./runs/<run-name>/` 目录下：
- 检查点文件：`ft_model.pth`
- 配置文件：与训练时使用的配置文件同名

### V1 推理

```bash
# 命令行推理
python inference.py \
    --source <源音频> \
    --target <参考音频> \
    --output <输出目录> \
    --diffusion-steps 25 \
    --checkpoint runs/<run-name>/ft_model.pth \
    --config runs/<run-name>/config_dit_mel_seed_uvit_whisper_small_wavenet.yml

# Web UI (语音转换)
python app_vc.py \
    --checkpoint runs/<run-name>/ft_model.pth \
    --config runs/<run-name>/config_dit_mel_seed_uvit_whisper_small_wavenet.yml

# Web UI (歌声转换)
python app_svc.py \
    --checkpoint runs/<run-name>/ft_model.pth \
    --config runs/<run-name>/config_dit_mel_seed_uvit_whisper_base_f0_44k.yml

# 实时语音转换 GUI
python real-time-gui.py \
    --checkpoint-path runs/<run-name>/ft_model.pth \
    --config-path runs/<run-name>/config_dit_mel_seed_uvit_xlsr_tiny.yml
```

### V1 推理参数说明

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `--diffusion-steps` | 扩散步数，质量最佳30-50，最快4-10 | 25 |
| `--length-adjust` | 长度调整因子，<1加速，>1减速 | 1.0 |
| `--inference-cfg-rate` | classifier free guidance 率 | 0.7 |
| `--f0-condition` | 是否启用F0调节（歌声转换设为True） | False |
| `--auto-f0-adjust` | 自动调整源音高至目标音高 | False |
| `--semi-tone-shift` | 音高移位（半音） | 0 |

---

## V2 模型

### V2 训练

```bash
# 仅训练 CFM (音色转换)
uv run accelerate launch train_v2.py \
    --dataset-dir /path/to/dataset \
    --run-name <run-name> \
    --batch-size 2 \
    --max-steps 1000 \
    --max-epochs 1000 \
    --save-every 500 \
    --num-workers 0 \
    --train-cfm

# 同时训练 CFM + AR (音色 + 口音/情感转换)
uv run accelerate launch train_v2.py \
    --dataset-dir /path/to/dataset \
    --run-name <run-name> \
    --batch-size 2 \
    --max-steps 1000 \
    --max-epochs 1000 \
    --save-every 500 \
    --num-workers 0 \
    --train-cfm \
    --train-ar
```

### V2 训练参数说明

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `--config` | 配置文件路径 | configs/v2/vc_wrapper.yaml |
| `--pretrained-cfm-ckpt` | 预训练CFM模型路径 | None (使用HF默认) |
| `--pretrained-ar-ckpt` | 预训练AR模型路径 | None (使用HF默认) |
| `--dataset-dir` | 数据集目录 | 必填 |
| `--run-name` | 运行名称 | 必填 |
| `--batch-size` | 批大小 | 2 |
| `--max-steps` | 最大步数 | 1000 |
| `--max-epochs` | 最大轮数 | 1000 |
| `--save-every` | 保存间隔(步数) | 500 |
| `--num-workers` | 数据加载线程数 | 0 |
| `--train-cfm` | 训练CFM模型 | 否 |
| `--train-ar` | 训练AR模型 | 否 |

### V2 检查点位置

训练完成后，检查点保存在 `./runs/<run-name>/` 目录下：
- CFM 模型：`CFM_epoch_XXXXX_step_XXXXX.pth`
- AR 模型：`AR_epoch_XXXXX_step_XXXXX.pth`

### V2 推理

```bash
# 命令行推理 (完整版: CFM + AR)
python inference_v2.py \
    --source <源音频> \
    --target <参考音频> \
    --output <输出目录> \
    --cfm-checkpoint-path runs/<run-name>/CFM_epoch_XXXXX_step_XXXXX.pth \
    --ar-checkpoint-path runs/<run-name>/AR_epoch_XXXXX_step_XXXXX.pth

# 命令行推理 (仅CFM，使用预训练AR)
python inference_v2.py \
    --source <源音频> \
    --target <参考音频> \
    --output <输出目录> \
    --cfm-checkpoint-path runs/<run-name>/CFM_epoch_XXXXX_step_XXXXX.pth

# Web UI 推理
python app_vc_v2.py \
    --cfm-checkpoint-path runs/<run-name>/CFM_epoch_XXXXX_step_XXXXX.pth \
    --ar-checkpoint-path runs/<run-name>/AR_epoch_XXXXX_step_XXXXX.pth
```

### V2 推理参数说明

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `--diffusion-steps` | 扩散步数 | 30 |
| `--length-adjust` | 长度调整因子 | 1.0 |
| `--intelligibility-cfg-rate` | 可懂度CFG率 | 0.7 |
| `--similarity-cfg-rate` | 相似度CFG率 | 0.7 |
| `--top-p` | AR模型采样top-p | 0.9 |
| `--temperature` | AR模型采样温度 | 1.0 |
| `--repetition-penalty` | 重复惩罚 | 1.0 |
| `--convert-style` | 是否转换口音/情感 | false |
| `--anonymization-only` | 仅匿名化模式 | false |
| `--compile` | 编译模型加速 | false |

---

## V1 vs V2 对比

| 特性 | V1 | V2 |
|------|-----|-----|
| 架构 | DiT + U-ViT | CFM + AR 双模型 |
| 音色转换 | ✅ | ✅ (CFM) |
| 口音/情感转换 | ❌ | ✅ (AR) |
| 实时转换 | ✅ (xlsr-tiny) | 即将支持 |
| 歌声转换 | ✅ | ✅ |
| 训练命令 | train.py | train_v2.py |
| 推理命令 | inference.py | inference_v2.py |

---

## 训练步数建议

根据数据集大小，推荐以下训练步数：

### V1 模型

| 数据集大小 | 推荐步数 | 说明 |
|-----------|---------|------|
| < 5 分钟 | 200-500 步 | 少量数据，快速微调 |
| 5-30 分钟 | 500-1000 步 | 默认推荐值 |
| 30 分钟 - 1 小时 | 1000-2000 步 | 充足数据 |
| 1+ 小时 | 2000-5000 步 | 大数据量，充分收敛 |

### V2 模型

V2 有两个独立模块（CFM 音色转换 + AR 口音/情感转换）：

| 数据集大小 | CFM 步数 | AR 步数 | 说明 |
|-----------|---------|--------|------|
| < 5 分钟 | 300-500 | 500-1000 | AR 需要更多数据 |
| 5-30 分钟 | 500-1000 | 1000-2000 | 默认推荐 |
| 30 分钟 - 1 小时 | 1000-2000 | 2000-3000 | 充足数据 |
| 1+ 小时 | 2000-4000 | 3000-5000 | 大数据量 |

**关键说明：**

- 官方最小要求：**100 步**（T4 GPU 约 2 分钟）
- V2 的 **AR 模块**需要更多训练数据才能有效学习语言内容，步数应为 CFM 的 **1.5-2 倍**
- `batch_size=2` 是默认值，GPU 显存允许可增大
- `save_interval=500` 表示每 500 步保存一次检查点

---

## 继续训练

V1 和 V2 都支持从上次停止处继续训练，只需使用相同的 `--run-name` 和配置参数即可自动恢复。

---

## 示例命令

### V1 完整示例

```bash
# 1. 训练
uv run python train.py \
    --config ./configs/presets/config_dit_mel_seed_uvit_whisper_small_wavenet.yml \
    --dataset-dir /mnt/data/resource/sound_data/彩玉/彩玉 \
    --run-name 彩玉v1 \
    --batch-size 2 \
    --max-steps 1000 \
    --max-epochs 1000 \
    --save-every 500 \
    --num-workers 0

# 2. 推理
python inference.py \
    --source examples/source/2.wav \
    --target /mnt/data/resource/sound_data/彩玉/彩玉/1.wav \
    --output ./outputs/ \
    --diffusion-steps 25 \
    --checkpoint runs/彩玉v1/ft_model.pth \
    --config runs/彩玉v1/config_dit_mel_seed_uvit_whisper_small_wavenet.yml
```

### V2 完整示例

```bash
# 1. 训练 (仅CFM)
uv run accelerate launch train_v2.py \
    --dataset-dir /mnt/data/resource/sound_data/彩玉/彩玉 \
    --run-name 彩玉v2 \
    --batch-size 2 \
    --max-steps 1000 \
    --max-epochs 1000 \
    --save-every 500 \
    --num-workers 0 \
    --train-cfm

# 2. 推理
python inference_v2.py \
    --source examples/source/2.wav \
    --target /mnt/data/resource/sound_data/彩玉/彩玉/1.wav \
    --output ./outputs/ \
    --cfm-checkpoint-path runs/彩玉v2/CFM_epoch_00000_01000.pth
```
